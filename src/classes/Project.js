class Project {
    constructor(title, category, url, desc, skills, imgs) {
      this.title = title;
      this.category = category;
      this.url = url;
      this.desc = desc;
      this.skills = skills;
      this.imgs = imgs;
    }
}

export default Project;